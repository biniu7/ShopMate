---
/**
 * Shopping Lists History page - przegląd i zarządzanie listami zakupów
 * Route: /shopping-lists
 */
import Layout from "@/layouts/Layout.astro";
import { ShoppingListsHistoryViewWrapper } from "@/components/shopping-lists/ShoppingListsHistoryViewWrapper";
import { Toaster } from "@/components/ui/toaster";

// Middleware sprawdza auth, więc tu mamy pewność że user jest zalogowany
---

<Layout title="Listy zakupów - ShopMate">
  <ShoppingListsHistoryViewWrapper client:load />
  <Toaster client:only="react" />
</Layout>
